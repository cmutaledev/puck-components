export interface TextBlockProps {
  text: string;
  size: string;
  textAlign: string;
  dimension: {
    width: string;
    height: string;
    lineHeight: number;
    margin: string;
    padding: string;
  };
  fontFamily: string;
}
